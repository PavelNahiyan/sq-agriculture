{
  "$schema": "https://railway.app/schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm ci && cd apps/api && npm ci && npm run build",
    "startCommand": "cd apps/api && npx prisma generate && node dist/main.js"
  },
  "deploy": {
    "numReplicas": 1,
    "restartDelaySecs": 10
  }
}
