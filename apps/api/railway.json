{
  "$schema": "https://railway.app/schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile",
    "buildPath": "/"
  },
  "deploy": {
    "numReplicas": 1,
    "restartDelaySecs": 10
  }
}
