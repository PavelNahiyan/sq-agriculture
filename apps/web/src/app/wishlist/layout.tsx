export const metadata = {
  title: 'My Wishlist - SQ Agriculture',
  description: 'View and manage your saved products',
};

export default function WishlistLayout({ children }: { children: React.ReactNode }) {
  return children;
}
