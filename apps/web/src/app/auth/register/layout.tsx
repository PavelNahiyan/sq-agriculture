export const metadata = {
  title: 'Register - SQ Agriculture',
  description: 'Create your SQ Agriculture account',
};

export default function AuthLayout({ children }: { children: React.ReactNode }) {
  return children;
}
