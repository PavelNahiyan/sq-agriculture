export const metadata = {
  title: 'Reset Password - SQ Agriculture',
  description: 'Reset your SQ Agriculture account password',
};

export default function AuthLayout({ children }: { children: React.ReactNode }) {
  return children;
}
