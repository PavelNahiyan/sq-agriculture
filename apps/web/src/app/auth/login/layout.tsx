export const metadata = {
  title: 'Login - SQ Agriculture',
  description: 'Sign in to your SQ Agriculture account',
};

export default function AuthLayout({ children }: { children: React.ReactNode }) {
  return children;
}
